public class st_max_area {
    //brute force
    public static void main(String[] args) {
        int width=1;
        int height[]={2,1,5,6,9,3};
        st_max_area(height,width);
    }
    public static int st_max_area(int height[],int width)
    {

       /*  int curr=0;
        int max=Integer.MIN_VALUE;
        for(int i=0;i<height.length;i++)
        {
            int area=height[i] * width;
            curr=area;
        if(curr>max)
        max=curr;
        }
        System.out.print(max);*/


        if(height[]==0)
        return -1;
        
}
}
